/**
 * Type shim for the generated `source-worker-runtime.js` — a pre-bundled,
 * worker-target build of the source shim (`src/runtime/source-shim.ts` plus
 * `@ontrove/extend/source`) embedded into the compiled binary and used by the Bun deploy
 * bundler (see `scripts/build-source-worker-runtime.mjs` and `src/lib/bundle.ts`).
 * The `.js` is a build artifact (git-ignored, generated by `build:vendor`); this
 * declaration lets `tsc` resolve the import without it.
 */
declare const source: string;
export default source;
