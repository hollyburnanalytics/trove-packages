/**
 * Type shim for the generated `mcp-worker-runtime.js` — a pre-bundled,
 * worker-target build of `@ontrove/mcp` embedded into the compiled binary and
 * used by the Bun deploy bundler (see `scripts/build-mcp-worker-runtime.mjs` and
 * `src/lib/bundle.ts`). The `.js` is a build artifact (git-ignored, generated by
 * `build:vendor`); this declaration lets `tsc` resolve the import without it.
 */
declare const source: string;
export default source;
